# 项目技术路线概要

本项目旨在基于**Faster R-CNN**框架，在DOTA遥感图像数据集中实现对飞机的**倾斜目标框 (Oriented Bounding Box - OBB)** 检测。核心挑战在于处理不同程度和类型的遮挡。我们将借鉴YOLT在大图处理、小目标/多尺度检测方面的经验，并重点引入**多样化的遮挡模拟**、**干扰物检测任务**，以及**基于高频特征注意力和关键点相对位置回归的检测框优化策略**，系统研究遮挡条件下遥感飞机检测的性能与挑战。

## 一、数据处理与准备

1.  **DOTA数据集适配：**
    *   **标注解析：** 解析DOTA提供的OBB标注（8个顶点坐标）。
    *   **图像分块 (Patching)：** 将DOTA的大尺寸原始图像（如4000x4000像素）切割成适合网络输入的较小图像块（例如1024x1024像素），并设置重叠区域（如100-200像素）。
    *   **标注转换：** 将原始图像中的OBB标注准确映射到各个图像块的坐标系下。

2.  **遮挡模拟与干扰物标注：**
    *   在数据预处理阶段，于图像块送入网络前，以参数化、可控的方式添加模拟遮挡。这不仅包括原有计划的**高斯光斑**，还将扩展到：
        *   **不同形状**：如椭圆高斯、模拟云层/阴影的不规则形状斑块等。
        *   **不同透明度与纹理**：引入alpha混合模拟半透明遮挡，或叠加随机纹理。
        *   **组合遮挡**：模拟多个小面积遮挡物同时出现的情况。
    *   **干扰物标注**：为这些模拟生成的遮挡物（干扰物）添加明确的类别标签（例如"interference"），使其成为一个独立的检测目标。
    *   **重要：** 真实飞机目标的标注（Ground Truth OBB）始终使用DOTA提供的原始无遮挡标注。

3.  **数据增强：**
    *   **基础增强：** 水平翻转、色彩抖动等。
    *   **旋转增强：** 对图像块进行随机旋转（如0°, 90°, 180°, 270°，或小角度随机旋转），并确保OBB顶点的坐标也进行相应几何变换。这对于任意朝向的飞机和OBB检测至关重要。

4.  **关键点标注准备 (针对高频特征回归)：**
    *   **关键点定义：** 直接利用DOTA数据集提供的OBB标注的4个顶点作为我们要回归的关键点。这些顶点本身就是重要的几何高频特征。
    *   **相对位置计算：** 对于每个飞机的真实OBB，计算其4个顶点相对于该OBB自身的归一化坐标。例如，可以计算相对于OBB中心点和OBB宽高（通过`cv2.minAreaRect`得到旋转矩形的中心、宽高和角度后计算）的偏移量 `((xi - cx)/w, (yi - cy)/h)`，或者相对于OBB左上角顶点和宽高的偏移。确保所选方法的一致性。这些归一化的相对坐标将作为关键点回归分支的训练目标。

## 二、模型构建与修改 (基于Faster R-CNN)

1.  **骨干网络 (Backbone Network) 与特征增强：**
    *   考虑改用用如 **ResNet-50** 或 **ResNet-101** 作为骨干网络。
    *   考虑集成 **特征金字塔网络 (FPN - Feature Pyramid Network)**，以有效处理DOTA数据集中目标的多尺度特性，尤其是小目标。
    *   **高频特征注意力机制 (High-Frequency Feature Attention)：** 在骨干网络的深层特征图之后，或FPN输出特征送入检测头之前，考虑引入注意力模块（如CBAM、SE-Net等空间或通道注意力机制）。目的是让网络在后续任务中能自适应地更加关注包含目标边缘、角点等高频信息的区域，抑制背景或遮挡区域的干扰。

2.  **区域提议网络 (RPN - Region Proposal Network)：**
    *   **锚点框 (Anchor Boxes)：**
        *   **类型：** 仍然使用**水平锚点框**。
        *   **设计：**
            *   分析DOTA飞机目标的最小外接水平矩形 (HBB) 的尺寸和长宽比分布。
            *   为FPN的每个特征层级（如P2-P6）设计合适的基础锚点尺寸 (base sizes/scales)，例如 `[32^2, 64^2, 128^2, 256^2, 512^2]` 对应的边长。
            *   根据飞机通常的细长形状，设计合适的长宽比 (aspect ratios)，例如 `[0.33, 0.5, 1.0, 2.0, 3.0]`。
    *   **RPN回归目标：** 仍然是4个参数 `(dx, dy, dw, dh)`，用于优化水平候选框。

3.  **RoI 头部 (RoI Head) - 核心修改区域：**
    *   **输入：** 接收由RPN生成的水平候选区域 (RoIs)，并通过RoI Align/Pooling提取特征。
    *   **分类器 (Classifier)：** 修改分类分支，使其能够预测飞机目标、背景以及新增的**干扰物类别** (如 "interference")。
    *   **边界框回归器 (Bounding Box Regressor for OBB)：**
        *   **输出层修改：** 将原Faster R-CNN中预测4个参数（对应HBB）的最终全连接层，修改为预测 **8个参数**。
        *   **参数化定义：** 这8个参数代表了目标倾斜框的4个顶点 `(gx_i, gy_i)` 相对于输入水平RoI（例如其左上角点 `(x_roi, y_roi)` 和宽高 `(w_roi, h_roi)`）的归一化偏移量。例如，对第 `i` 个顶点：
            *   `t_xi = (gx_i - x_roi) / w_roi`
            *   `t_yi = (gyi - y_roi) / h_roi`
            *   （或采用DOTA论文中基于RoI四个顶点的偏移定义方式，确保一致性即可）。
    *   **关键点相对位置回归器 (Keypoint Relative Position Regressor)：**
        *   新增一个与OBB回归器和分类器并行的回归分支。
        *   **输出层：** 预测N个关键点相对于预测出的OBB框的归一化内部相对位置。对于使用DOTA的4个顶点作为关键点，则输出 `4 * 2 = 8` 个值 `(nrx_i, nry_i)`，其中i遍历4个顶点。
        *   **回归目标：** 这些值对应于关键点在预测OBB框内的归一化坐标（例如，范围在[0,1]或[-0.5, 0.5]，取决于归一化方式）。

## 三、损失函数与训练

1.  **RPN损失：** 保持标准Faster R-CNN的RPN损失（分类损失 + 水平框回归损失）。
2.  **RoI头部损失 (多任务损失)：**
    *   **分类损失：** 标准交叉熵损失，作用于飞机、背景及新增的干扰物类别。
    *   **OBB回归损失：**
        *   **目标值计算：** 对于每个与真实OBB匹配的RoI，根据上述参数化方法计算其对应的8个目标回归值 `(target_tx1, target_ty1, ..., target_tx4, target_ty4)`。
        *   **损失函数：** 使用 **Smooth L1 Loss** (或其他合适的回归损失) 计算模型预测的8个参数与这8个目标回归值之间的差异。
    *   **关键点相对位置回归损失 (Keypoint Relative Position Regression Loss)：**
        *   **目标值计算：** 使用在数据准备阶段计算好的、关键点相对于其真实OBB的归一化坐标作为训练目标。
        *   **损失函数：** 使用 **Smooth L1 Loss** (或其他合适的回归损失) 计算模型预测的N*2个相对坐标与真实值之间的差异。
    *   **几何一致性与联合优化 (探索性)：**
        *   为进一步提升遮挡情况下的定位鲁棒性，可探索引入额外的损失项，以鼓励预测的OBB与预测的内部关键点之间的几何一致性。例如，一个损失项可以惩罚那些导致预测关键点（根据其相对位置和预测OBB计算出的绝对位置）偏离其应有位置（可能通过另一个弱监督的绝对关键点预测或基于特征图的直接估计）的OBB预测。这有助于通过内部结构约束来"反推"或优化检测框。

## 四、推理与后处理

1.  **模型推理：** 将带有模拟遮挡（或无遮挡）的测试图像块输入训练好的模型。
2.  **OBB参数解码：** 将模型回归头输出的8个归一化偏移量转换回图像块坐标系下OBB的绝对顶点坐标。
3.  **关键点相对位置解码：** 将关键点回归分支输出的 `N*2` 个归一化值，结合预测的OBB，解码出各个关键点在图像块坐标系下的绝对位置（如果需要直接可视化或使用这些点）。
4.  **结果合并与NMS：**
    *   将从各个图像块中检测到的OBB（以及可能的干扰物检测框）坐标转换回原始大图的坐标系。
    *   **倾斜非极大值抑制 (Oriented NMS)：** 使用适用于OBB的NMS算法（基于旋转框的IoU计算）来去除冗余检测结果。DOTA论文建议的OBB NMS阈值为0.1。对干扰物检测结果也应进行NMS处理。
5.  **中心点计算：** 根据最终预测的OBB顶点坐标，计算出任务要求的飞机中心点。
6.  **基于关键点的检测框优化 (探索性)：**
    *   如果采用联合优化损失函数，检测框的优化是端到端在训练中完成的。
    *   如果未采用或希望进一步增强，可以设计后处理步骤：利用预测出的关键点在预测OBB内的相对位置，结合对这些关键点绝对位置的（可能更可靠的）估计（尤其对可见关键点），来调整或修正初始预测的OBB。例如，若预测的机头关键点显著超出了预测OBB的某个边界，则可以据此调整该边界。

## 五、实验设计与分析

1.  **基线建立：** 在无遮挡的原始DOTA飞机数据上训练并评估模型性能。
2.  **干扰物检测评估：** 评估模型检测和分类干扰物的能力（如精确率、召回率），及其对飞机检测性能的影响。
3.  **遮挡影响分析：**
    *   系统地改变**不同类型和参数**的遮挡（高斯光斑、不规则形状、透明度、组合遮挡等）。
    *   根据目标大小（尺度）对飞机进行分组。
    *   评估在不同遮挡程度和目标尺度下，模型的检测性能（如mAP for OBB, 中心点预测误差等）。
4.  **高频特征利用与关键点回归有效性评估：**
    *   评估注意力机制的引入对检测性能的提升。
    *   评估关键点相对位置回归分支的准确性。
    *   量化基于关键点的检测框优化策略（无论是通过联合损失还是后处理）对最终OBB定位精度（尤其是在遮挡条件下）的改善程度。

## 六、技术栈

*   **深度学习框架：** PyTorch。
*   **核心库：** OpenCV, NumPy, Matplotlib/Seaborn。
*   **目标检测库参考：** 可借鉴MMDetection, Detectron2等成熟框架的实现思路和模块。 