# 遥感飞机检测APP 打包说明

## 概述

本文档提供了多种打包和部署遥感飞机检测APP的方法，包括：

1. **便携式包** - 最简单的分发方式
2. **可执行文件** - 使用PyInstaller打包
3. **Docker容器** - 容器化部署
4. **Python包** - 通过pip安装

## 方法一：便携式包（推荐）

### 步骤1：运行打包脚本

```bash
python build_exe.py
```

选择选项2或3来创建便携式包。

### 步骤2：分发文件

打包完成后，在`dist/遥感飞机检测APP/`目录下会包含：

```
遥感飞机检测APP/
├── app_streamlit.py          # 主应用文件
├── config.yaml              # 配置文件
├── requirements.txt          # 依赖列表
├── README.md                # 说明文档
├── 启动APP.bat              # 启动脚本
├── 安装依赖.bat              # 安装脚本
├── 使用说明.txt              # 使用说明
├── src/                     # 源代码目录
│   ├── __init__.py
│   ├── image_utils.py
│   ├── yolo_detector.py
│   ├── visualization.py
│   ├── metrics.py
│   └── main_cli.py
└── models_yolo/             # 模型目录（如果存在）
    ├── yolov8n-obb.pt
    ├── my_circle_spot_model.pt
    └── my_ellipse_spot_model.pt
```

### 步骤3：用户使用

用户只需要：

1. 双击运行`安装依赖.bat`安装Python依赖
2. 双击运行`启动APP.bat`启动应用
3. 在浏览器中访问 http://localhost:8501

## 方法二：可执行文件

### 步骤1：安装PyInstaller

```bash
pip install pyinstaller
```

### 步骤2：运行打包脚本

```bash
python build_exe.py
```

选择选项1或3来创建可执行文件。

### 步骤3：查找生成的文件

可执行文件将生成在`dist/遥感飞机检测APP/`目录下。

## 方法三：Docker容器部署

### 步骤1：构建Docker镜像

```bash
docker build -t remote-sensing-app .
```

### 步骤2：运行容器

```bash
docker run -d \
  --name remote-sensing-app \
  -p 8501:8501 \
  -v $(pwd)/models_yolo:/app/models_yolo \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/results:/app/results \
  remote-sensing-app
```

### 步骤3：使用Docker Compose（推荐）

```bash
docker-compose up -d
```

访问 http://localhost:8501 使用应用。

## 方法四：Python包安装

### 步骤1：构建包

```bash
python setup.py sdist bdist_wheel
```

### 步骤2：安装包

```bash
pip install dist/remote-sensing-detection-app-1.3.0.tar.gz
```

### 步骤3：运行应用

```bash
streamlit run app_streamlit.py
```

## 系统要求

### 便携式包和可执行文件
- Windows 10/11
- Python 3.8+
- 4GB+ 内存
- 支持CUDA的显卡（可选）

### Docker部署
- Docker 20.10+
- Docker Compose 2.0+
- 4GB+ 内存

## 故障排除

### 常见问题

1. **依赖安装失败**
   - 检查Python版本是否为3.8+
   - 尝试使用国内镜像源：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple/`

2. **模型文件缺失**
   - 确保`models_yolo/`目录包含必要的.pt文件
   - 可以从Ultralytics官方下载预训练模型

3. **端口被占用**
   - 修改`app_streamlit.py`中的端口号
   - 或使用`netstat -ano | findstr 8501`查找占用进程

4. **中文显示问题**
   - 确保系统安装了中文字体
   - 检查Python环境的编码设置

### 日志查看

- **Streamlit日志**：查看控制台输出
- **Docker日志**：`docker logs remote-sensing-app`
- **系统日志**：Windows事件查看器

## 性能优化

### 模型优化
- 使用更小的模型（如yolov8n-obb）
- 启用GPU加速（需要CUDA）
- 调整推理参数（conf_threshold, iou_threshold）

### 系统优化
- 增加内存分配
- 使用SSD存储
- 关闭不必要的后台程序

## 安全考虑

1. **网络安全**
   - 在生产环境中使用HTTPS
   - 配置防火墙规则
   - 限制访问IP

2. **数据安全**
   - 定期备份模型文件
   - 加密敏感数据
   - 实施访问控制

## 更新和维护

### 应用更新
1. 备份当前版本
2. 下载新版本
3. 替换文件
4. 测试功能

### 模型更新
1. 将新模型放入`models_yolo/`目录
2. 更新`config.yaml`配置
3. 重启应用

## 技术支持

如遇到问题，请：

1. 查看本文档的故障排除部分
2. 检查应用日志
3. 联系开发团队

---

**注意**：本应用仅供学习和研究使用，请勿用于商业用途。 