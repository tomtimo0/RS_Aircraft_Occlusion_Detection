# 遥感飞机目标检测（含高斯光斑遮挡模拟）课程设计技术方案 V1.0

**项目组长：** （你的名字）
**项目成员：** （成员A, B, C, D）
**当前周：** 第十四周周末
**答辩时间：** 第十九周
**核心目标：** 构建高斯光斑模拟的飞机部分遮挡场景测试数据集，实现遮挡条件下的飞机目标自动检测（旋转矩形框及中心点），并分析遮挡程度、目标尺寸等对结果的影响。优先利用现有成果。

---

## I. 项目整体规划与时间安排

| 周次  | 主要任务                                                                 | 负责人 | 产出物                                                                 |
| :---- | :----------------------------------------------------------------------- | :----- | :--------------------------------------------------------------------- |
| **14 (本周)** | 方案最终确定，环境搭建，DOTA数据集下载与初步处理，学习YOLOv8-OBB框架基础。       | 全体   | 技术方案V1.0，Anaconda/PyTorch环境，DOTA数据集，YOLOv8-OBB安装与示例运行。 |
| **15** | **核心任务1：** 高斯光斑模拟与叠加实现。 **核心任务2：** DOTA飞机数据精细准备 (切割与转换)。 | 成员A,B | Python高斯光斑生成与叠加代码，DOTA飞机图像子集，YOLO-OBB格式标注文件。         |
| **16** | **核心任务3：** 构建遮挡测试数据集与遮挡度量计算。 **核心任务4：** 训练基线YOLOv8-OBB模型。 | 成员C,D | 遮挡飞机测试集，遮挡度量指标计算脚本，初步训练的YOLOv8-OBB模型。         |
| **17** | **核心任务5：** 模型评估与分析（遮挡度、尺寸影响），可选：遮挡数据增强训练。               | 全体   | 初步性能分析报告（含图表），可选：使用遮挡增强训练的模型。                   |
| **18** | 报告撰写，PPT制作，代码整理与演示准备。                                       | 全体   | 课程设计报告初稿，答辩PPT初稿，项目代码库。                               |
| **19** | 报告与PPT最终修改，答辩演练与最终答辩。                                      | 全体   | 最终版报告与PPT。                                                        |

---

## II. DOTA数据集与飞机目标特性分析

**A. DOTA数据集概览**

* **数据集：** DOTA (Dataset for Object deTection in Aerial images)。推荐使用 **DOTA-v1.0**。
    * 15类别，2806张图像，188,282实例。
* **图像特性：** 尺寸极大，来源多样，需切割成小块（如1024x1024）进行训练。
* **标注格式：** 旋转矩形框（Oriented Bounding Boxes - OBB），原始格式为四边形8参数 `x1 y1 x2 y2 x3 y3 x4 y4`。

**B. DOTA中“飞机”目标特性**

* **实例数量：** 约 11,281 个。
* **尺寸分布：** 跨度大，从小到大均有覆盖，大部分属于中小型目标。
* **朝向分布：** 任意朝向，分布均衡。
* **自然遮挡：** **非常罕见**，适合作为模拟遮挡的基础。
* **基准性能：** 在 DOTA v1.0 上，Faster R-CNN OBB (FR-O) 对 'plane' 类别的 AP 约为 **79.42%**，可作为参考。

**C. 数据准备流程**

1.  **下载 DOTA-v1.0：** 从官方网站获取图像和标注。
2.  **筛选飞机图像：** 提取包含 'plane' 类别的图像及其标注。
3.  **切割图像：** 使用 DOTA 官方提供的 `DOTA_devkit/prepare_DOTA.py` 或类似脚本，将大图像切割成 1024x1024 (可调整) 的小块，并生成对应的标注文件。注意处理跨块的目标。
4.  **转换标注格式：** 使用 Ultralytics 提供的 `convert_dota_to_yolo_obb` 脚本或自定义脚本，将 DOTA 8参数标注转换为 YOLO-OBB 格式 (`class_index x1 y1 x2 y2 x3 y3 x4 y4`，归一化坐标)。
5.  **创建数据集 `yaml` 文件：** 定义训练集、验证集路径和类别信息（仅 'plane'）。

---

## III. 高斯光斑模拟部分遮挡技术

**A. 生成参数化高斯光斑**

1.  **原理：** 生成一个 2D 椭圆高斯函数分布图，模拟光斑。
    * $I(x,y) = A \cdot \exp \left( - \left( \frac{x'^2}{2\sigma_x^2} + \frac{y'^2}{2\sigma_y^2} \right) \right)$ (其中 $x', y'$ 是旋转后的坐标)。
2.  **关键参数：**
    * 中心 $(x_0, y_0)$：光斑相对于飞机的位置。
    * 振幅 $A$：光斑亮度 (0-255)。
    * 标准差 $\sigma_x, \sigma_y$：光斑尺寸和形状。
    * 旋转角 $\theta$：光斑方向。
3.  **实现 (Python + NumPy/OpenCV)：**
    * 使用 `np.meshgrid` 生成坐标网格。
    * 实现 2D 椭圆高斯函数，根据输入参数生成强度矩阵。
    * 可以参考 `pygaussfit.twodgaussian` 的实现。

**B. 光斑叠加到图像**

1.  **生成 RGBA 光斑图像：**
    * 将生成的高斯强度矩阵归一化到 [0, 1]。
    * 创建一个 RGBA 图像 (NumPy 数组 `h x w x 4`)。
    * RGB 通道设置为光斑颜色（如白色 `[255, 255, 255]`）。
    * Alpha 通道设置为归一化后的高斯强度乘以 255。
2.  **叠加 (Alpha Blending)：**
    * 确定光斑在飞机图像上的叠加位置。
    * 使用 OpenCV 或 PIL 进行 Alpha 混合：
        `Output = Glint_RGB * Glint_Alpha + Background_RGB * (1 - Glint_Alpha)`
    * 确保只在目标区域或其附近叠加。

---

## IV. 遮挡测试数据集构建与度量

**A. 测试集构建流程**

1.  **选取基础集：** 从 DOTA 验证集中选取一定数量（如 100-200 张）包含飞机的清晰图像。
2.  **定义遮挡等级：** 设计不同的高斯光斑参数组合（不同尺寸、强度、位置、旋转），以产生不同程度的遮挡效果。
3.  **循环生成：**
    * 对每张基础图像中的每个飞机实例：
        * 应用多种预设的高斯光斑参数组合。
        * 生成并叠加光斑。
        * **计算遮挡度量指标** (见下文)。
        * 保存遮挡后的图像，**其标注仍使用原始的、无遮挡的 OBB 坐标**。
        * 记录图像文件名、原始飞机ID、光斑参数、遮挡度量值。

**B. 遮挡度量指标 (Occlusion Degree Metric)**

* **核心指标：遮挡 OBB 面积百分比 (OOAP - Occluded OBB Area Percentage)**
    1.  **定义光斑有效边界：** 将高斯光斑在某个阈值（如 $2\sigma$ 或 $3\sigma$）处定义为椭圆边界。
    2.  **使用 Shapely 库：**
        * 将飞机 OBB (8参数) 转换为 `shapely.geometry.Polygon` 对象。
        * 将光斑有效边界（椭圆）近似为 `shapely.geometry.Polygon` 对象。
        * 计算两个多边形的交集面积：`Intersection_Area = Glint_Polygon.intersection(Aircraft_OBB_Polygon).area`。
        * 计算 `OOAP = (Intersection_Area / Aircraft_OBB_Polygon.area) * 100%`。
* **辅助指标 (可选)：**
    * **峰值光斑强度 (PGIT):** 即生成光斑时使用的振幅 $A$。

---

## V. 目标检测模型与训练

**A. 推荐框架与模型**

* **框架：** **Ultralytics YOLOv8** (因为它对 OBB 有官方支持且易用)。
* **模型：** **`yolov8n-obb.pt`**。
* **理由：** 它是最小、最快的 YOLOv8 OBB 模型，且 **已在 DOTA 上预训练**，非常适合基础薄弱、时间有限的团队作为起点。

**B. 训练策略**

1.  **基线模型训练：**
    * 使用准备好的 DOTA 飞机训练集（无模拟遮挡）。
    * 微调 `yolov8n-obb.pt` 模型。
    * 获取一个在标准 DOTA 飞机数据上的性能基准。
    ```bash
    yolo obb train data=dota_planes.yaml model=yolov8n-obb.pt epochs=50 imgsz=1024 device=0
    ```
2.  **遮挡鲁棒模型训练 (可选，但推荐尝试)：**
    * **方法1 (推荐):** 在训练过程中**实时 (On-the-fly) 应用高斯光斑增强**。
        * 这需要修改 YOLOv8 的数据加载器或增强函数，加入你们写好的高斯光斑生成与叠加代码。这可能有些挑战，需要研究 YOLOv8 的代码结构。
    * **方法2 (更简单):** **预先生成增强的训练集**。
        * 对一部分 DOTA 飞机训练图像应用随机的高斯光斑模拟，生成一个新的、更大的、包含遮挡样本的训练集。
        * 用这个混合数据集训练模型。
    * 比较鲁棒模型和基线模型在遮挡测试集上的性能。

---

## VI. 性能评估与分析

**A. 评估指标**

* **核心指标：mAP@0.5 (Mean Average Precision at IoU 0.5):** 这是 OBB 检测的标准指标。
* **辅助指标：** Precision, Recall, F1-Score。
* **IoU 计算：** 使用 **Shapely** 库计算两个旋转矩形（多边形）的交并比 (IoU)。
    * `IoU = Area(Intersection) / Area(Union)`。

**B. 影响因素分析**

1.  **遮挡程度影响：**
    * 将遮挡测试集按 OOAP 分组（如 0-20%, 21-40%, ...）。
    * 分别计算每个分组的 mAP。
    * 绘制 mAP vs OOAP 的关系图，分析性能随遮挡程度的变化。
2.  **目标尺寸影响：**
    * 将测试集中的飞机按 OBB 面积分为小、中、大三组。
    * 分别计算每个尺寸组的 mAP (在无遮挡和有遮挡情况下)。
    * 分析模型对不同尺寸飞机的检测能力，以及遮挡对不同尺寸飞机的影响。
3.  **定性分析：**
    * **可视化结果：** 在测试图像上绘制检测框（包括成功和失败的例子）。
    * **分析失败案例：** 找出模型在哪些遮挡情况下容易失败（漏检、定位不准、误检），并尝试分析原因。

**C. 教师建议的考量**

* **干扰检测 (光斑作为目标)：** 建议**不作为核心任务**。这会增加项目复杂性。优先让飞机检测器对光斑鲁棒。如果鲁棒性训练效果不佳，可以考虑将其作为失败案例分析或未来工作。
* **高频特征注意力/回归：** 这是高级研究方向。对于本团队，**建议不直接实现**。可以在报告中讨论其潜力，并指出 SOTA 模型（如 YOLOv8）中的 FPN、C2f 等结构在一定程度上融合了多尺度特征。如果想尝试，可研究 CBAM 等轻量级注意力模块集成到 YOLOv8 的可能性（有一定门槛）。**核心是先完成基本任务。**

---

## VII. 预期成果与报告要求

* **代码：** DOTA 数据预处理脚本、高斯光斑模拟与叠加脚本、遮挡测试集生成脚本、YOLOv8-OBB 训练与评估脚本。
* **数据集：** 生成的含高斯光斑遮挡的飞机测试集。
* **模型：** 训练好的基线模型和（可选）鲁棒模型权重。
* **报告：** 详细的课程设计报告，包含：
    * 项目背景与任务描述。
    * 数据集介绍与准备过程。
    * 高斯光斑模拟方法与实现。
    * 目标检测模型 (YOLOv8-OBB) 介绍。
    * 训练与评估细节。
    * **核心：** 实验结果与分析（遮挡度影响、尺寸影响、失败案例）。
    * 结论与未来工作展望。
* **PPT & 演示：** 清晰展示项目流程、核心工作、结果分析和个人贡献。

---

## VIII. 风险与预案

* **风险1：** 修改 YOLOv8 加入实时光斑增强难度大。
    * **预案：** 采用预生成增强训练集的方法。
* **风险2：** 模型训练时间长，资源不足。
    * **预案：** 使用 `yolov8n-obb.pt`，适当减少训练 `epochs`，优先保证流程跑通和结果分析。
* **风险3：** 遮挡度量或光斑模拟效果不理想。
    * **预案：** 尽早实现并进行小规模测试，根据反馈快速迭代调整参数或方法。